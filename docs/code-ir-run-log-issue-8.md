# Issue #8 implementation and verification log

## Frozen control (phase 0)

- Worktree `spike/code-ir-design`, control revision `5f2c5e490c93652619b3661faa42a439981517fb`; no control source edits. Engram protocol checkout `40d4f58efaaa2b8629c994045e6ea2cdae9f9d6b` is read-only.
- Frozen same-engine index versions are TypeScript `CURRENT_INDEX_VERSION=2` and Rust `CURRENT_INDEX_VERSION=2`. Control is the checkout's ordinary default index/search invocation (no experimental IR flags); paired replay protocol fixes CPU/direct-hybrid, `local/potion-code-16m-v2`, source selection, eight queries, and raw top-10 separately within each engine/language arm.
- Fixture manifests SHA-256: Go `945fa2aa1a61ce665dc3750cb36dfed63aa9e687c3bb4848ad09df47cea3b467`, Python `a3e565a460e3cc6b37fa600afa47589053f0d7c4e75b71744e803a25240edf30`, Rust `e5edb7912413905c564016e8c61065414228f97daeec604c04356ae6b8fad166`, TypeScript `f3e14f7d7c327d631e8e640a236ef6da62da2519dcbb1b3e426bb29a0419a1c4`.
- Frozen selected-source-set digests (sorted manifest path + SHA-256 tuples; 10 Go / 8 Python / 14 Rust / 8 TypeScript files): Go `1f570e8ceb6517fffabc7322558199e9e0971d06776dfe3a0fbc83420e138384`, Python `d730841cbcc679aee47b139c6a1f2bfef8c138b56a429db4557d6c0da95b5aa1`, Rust `66ac9ba7a9137d29595900ec028e62999095a595536e41b9d8d82276b54ae65b`, TypeScript `9ca5370546557afbdc8a45c6ab93f7b62cbd2842176b22afaebe7ef539bbbda4`.
- Qrels SHA-256: Go `0d89687ffd9aac8e6688e9c0931aeb27cca55b99d04f6379ecc5b9bba541c606`, Python `5daba6da79055aba67d8a7cf8f866b1662f89cde905da3703c08f06975a3b279`, Rust `b6c63eb8b7741ec22d1dd7f66e71489315a803fd4d2c083e95ca2d092db262b9`, TypeScript `af738283d787b2198c2e125cfd5346f4aa88a05d89ecf4926e84821f670d936e`.
- Four lanes: 38 Go, 33 Python, 39 Rust (six grade-0 module units), 33 TypeScript. Use `../engram/benchmarks/semantic_search/MULTILANGUAGE_PROTOCOL.md` for same-engine, fresh-directory paired runs, keeping CPU/direct hybrid and `local/potion-code-16m-v2` fixed. Existing default TypeScript indexing follows `extractor.ts` -> `pipeline/indexing/index.ts` -> `storage/zvec.ts`; Rust follows `extraction/code.rs` -> indexing -> workspace generation. Neither old lexical/vector result nor graph sidecar is an IR record.
- Independent baseline checks: `npm run build`, `node --test test/unit/extraction/*.test.mjs`, `cargo test -p zg-codegraph` (run from `rust/`). Candidate is selected only with an explicit IR mode; `off` remains the default.
- Engram MCP gateway and Serena tools are not exposed to this session; the frozen Engram protocol and linked design were read directly. No sibling fixture or qrels changes.
- SCIP sequencing (per the newly updated design/plan): phase 3a is **deferred** until syntax-only IR meets the independent conformance gate. The optional importer will be a consumer of source-attested SCIP output and may not gate phases 4–6. `protoc` exists but `scip-typescript`, `scip-python`, `scip-rust`, and `scip-go` are not installed in this environment; no producer output or semantic precision result is claimed.

## Milestones

Record verified results here as stages complete. A conformance pass is not a ranking improvement.

## Phase 1–2 current evidence

- TypeScript build passed. New serialized mini-file tests passed for Go/Python/Rust/TypeScript, expected bytes, stale bytes, invalid version/status, astral Unicode, CRLF, duplicate names and opaque/failed files.
- Current frontend stamps are TypeScript `web-tree-sitter-ir-v1.3` and Rust `tree-sitter-rust-ir-v1.2`; stamps are bumped when extraction semantics change so a content-addressed snapshot cannot alias different records under the same frontend identity.
- A repeated publisher invocation detected an immutable generation collision when the extraction implementation changed before the initial version bump. After bumping both frontend versions, the new snapshot IDs published and reopened cleanly; the source-set digest remained unchanged.
- Rust `zg-code-ir` tests passed for four grammar frontends and stale source rejection. A Rust validator example accepted a TypeScript-produced JSON snapshot against the original fixture sources (cross-runtime decoder/validator smoke check).
- Both runtime frontends were run against the frozen manifests. The **TypeScript/web-tree-sitter** and **Rust/tree-sitter** frontends each report provisional unique symbol-name matches Go 38/38, Python 33/33, Rust 33/39, TypeScript 33/33. For both, Python and TypeScript match the frozen manifest's 1-based start/end *lines* for 33/33 units each; Rust matches 31/39 line spans, with 8 line-span mismatches. The Go manifest does not contain line spans. These are not exact byte-span/kind matches. In Rust, the six grouped `module-declarations` entries are retained as 13 individual source-backed module units (5+3+2+1+1+1); three other grouped impl/type names are ambiguous. No independently adjudicated per-language byte-span/kind/call-site inventory exists, so phase 3 is not passed and no lane is eligible for promotion.
- TypeScript/web-tree-sitter emitted refs round-trip through its validator on frozen files: Go 48 units/81 facts; Python 41/79; Rust 63/146; TypeScript 46/71. Rust/tree-sitter emitted: Go 48/81; Python 41/79; Rust 63/146; TypeScript 55/80. All selected fixture files parsed as complete. Calls emitted by either frontend are unresolved only: Go 43, Python 46, Rust 97, TypeScript 33; the manually labeled relation-site denominator is unavailable. No type-resolved facts are emitted by either runtime.
- The TypeScript corpus differs by frontend on the TypeScript lane (46 units/71 facts versus 55/80); this is an explicit declaration/fact coverage discrepancy to adjudicate, not a success inferred from a larger count.
- Sidecar smoke test completed in a temporary directory: `off` produces no sidecar, `shadow` writes a validated immutable generation, and `projection` writes a same-snapshot derived record set. It is a standalone opt-in tool, not integrated into either default search/index lifecycle.

## Phase 4–5 current evidence

- TypeScript standalone publisher wrote and reopened same-snapshot candidate projections on all frozen source selections: Go 48 projection windows, Python 41, Rust 63, TypeScript 46. Each count equals the emitted units here; a dedicated 3,600-character Unicode/CRLF test forces multiple windows and verifies exact contiguous source-byte round trips. The projection schema links each window to its unit, group, source refs, projection version and optional model identity.
- Rust `zg-code-ir::publish_shadow` writes source bytes separately under an immutable content-addressed generation and atomically publishes `active.json` last. Restart and idempotent publication are tested. Rust engine indexing/search is not wired to query this sidecar; TypeScript's sidecar likewise remains a separate opt-in utility. No default search or returned evidence path uses projections.
- Cross-runtime checks pass both directions: Rust validates all four complete serialized TypeScript examples; TypeScript validates a fresh Rust-produced JSON snapshot including multibyte Unicode and CRLF. `npm run code-ir:cross-runtime` reports Rust producer / TypeScript consumer success.

## Verification and gates

- `npm run check` passed on the final TypeScript implementation: lint, format, typecheck, full root/unit/integration/e2e suites, 90.08% aggregate statement/line coverage (84.86% branches), and package tests. This includes all new IR and existing lexical regressions.
- Rust: `cargo test -p zg-code-ir` passed 5 tests; `cargo clippy -p zg-code-ir --all-targets -- -D warnings` and `cargo fmt --check` passed. `cargo test -p zg-codegraph` passed 17 tests. `cargo test -p zg-engine` passed 463 tests across unit/integration binaries, 10 ignored; on this macOS host it required `CXXFLAGS="-isystem $(xcrun --show-sdk-path)/usr/include/c++/v1"` for the llama.cpp native build.
- Phase 3 remains **not passed**: exact byte-span+kind coverage and manually annotated relation coverage are unavailable from the frozen manifests. Observed provisional line-span figures and all six Rust aggregate module groups are recorded above. Do not enable any per-language retrieval lane.
- Paired qeval is blocked at wiring: the projection artifacts are not ingested by either engine's search index, so an IR-backed retrieval arm does not exist to compare against same-engine controls. No per-query or ranking metric is claimed; no lane is promoted.
- SCIP phase 3a was not started after re-reading the user's updated sequence: syntax conformance has not passed. Producer commands are absent (`protoc` only), and no index artifact/provenance policy was available. No SCIP precision, freshness or ranking claim is made.
